logging = False;   #We use to log an activity to the server.
logging_in = True;  #Indicates if a user is currently logged in
name = "Tayo";
if logging_in:
    if logging:
        print(f"{name}, is logging in!");
        
print(f"Hello & Welcome, {name}!");